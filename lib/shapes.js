// we will setup different files here

// an example of a class constructor
class Car {
    constructor(make, model, color) {
      this.make = make;
      this.model = model;
      this.color = color;
    }
  }

export class ClassName {
    // class definition
  }